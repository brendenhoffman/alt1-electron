name: Build/release

on:
  push:
    branches:
      - arroquw-dev

permissions:
  contents: write

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v5

      - name: Install dependencies
        run: sudo apt install pkg-config libxcb-shm0-dev libxcb-composite0-dev libxcb-ewmh-dev libxcb-record0-dev libxcb-shape0-dev
        if: matrix.os == 'ubuntu-latest'

      - name: Install Node.js, NPM
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Build (no publish)
        run: |
          npm ci
          npm run build
          npx electron-builder -l --publish never

      - name: Move nightly tag via GitHub API
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api -X PATCH \
            repos/${{ github.repository }}/git/refs/tags/nightly \
            -f sha="${GITHUB_SHA}"

      - name: Publish rolling Nightly release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create nightly --title "Nightly" --latest --notes "Automated build" || true
          gh release upload nightly release/*.AppImage --clobber
